        winbind use default domain = <%= @use_default_domain ? 'Yes' : 'No' %>
        winbind nss info = <%= @nss_info %>
        winbind refresh tickets = <%= @refresh_tickets ? 'Yes' : 'No' %>
        winbind offline logon = <%= @offline_logon ? 'Yes' : 'No' %>
        template homedir = <%= @template_homedir %>
        template shell = <%= @template_shell %>
